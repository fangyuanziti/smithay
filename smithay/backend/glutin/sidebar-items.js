initSidebarItems({"enum":[["GlutinInputError","Errors that may happen when driving the event loop of `GlutinInputBackend`"]],"fn":[["init_headless_renderer","Create a new `GlutinHeadlessRenderer` which implements the `OpenglRenderer` graphics backend trait"],["init_headless_renderer_from_builder","Create a new `GlutinHeadlessRenderer`, which implements the `OpenglRenderer` graphics backend trait, with a given already configured `HeadlessRendererBuilder` for customization"],["init_windowed","Create a new `glutin` `Window`. Returns a `GlutinWindowedRenderer` implementing the `OpenglRenderer` graphics backend trait and a `GlutinInputBackend` implementing the `InputBackend` trait."],["init_windowed_from_builder","Create a new `glutin` `Window` with a given already configured `WindowBuilder` for customization. Returns a `GlutinWindowedRenderer` implementing the `OpenglRenderer` graphics backend trait and a `GlutinInputBackend` implementing the `InputBackend` trait."],["init_windowed_renderer","Create a new `GlutinWindowedRenderer`, which implements the `OpenglRenderer` graphics backend trait"],["init_windowed_renderer_from_builder","Create a new `GlutinWindowedRenderer`, which implements the `OpenglRenderer` graphics backend trait, with a given already configured `WindowBuilder` for customization."]],"struct":[["GlutinHeadlessRenderer","Headless Opengl Context created by `glutin`. Implements the `OpenglGraphicsBackend` graphics backend trait."],["GlutinInputBackend","Abstracted event loop of a `glutin` `Window` implementing the `InputBackend` trait"],["GlutinWindowedRenderer","Window with an active Opengl Context created by `glutin`. Implements the `OpenglGraphicsBackend` graphics backend trait."]]});